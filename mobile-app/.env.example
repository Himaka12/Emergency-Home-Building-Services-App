# Leave this unset or keep the default while developing.
# The app derives the API host from the Expo dev server so emulator and physical phone both work.
EXPO_PUBLIC_API_URL=http://10.0.2.2:5000/api

# Manual override example:
# EXPO_PUBLIC_API_URL=http://YOUR_LOCAL_IP:5000/api
