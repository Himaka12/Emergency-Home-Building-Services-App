import { Text } from 'react-native';
import ScreenContainer from '../common/ScreenContainer';

const RateWorkerScreen = () => {
  return (
    <ScreenContainer>
      <Text>Rate Worker</Text>
      <Text>Ratings are allowed after completed bookings.</Text>
    </ScreenContainer>
  );
};

export default RateWorkerScreen;
